# Publish log

Append-only. One line per cron run: what it found, what it did, nothing else. Full context for a specific post lives in that post's own queue file.

**Format:** `YYYY-MM-DD HH:MM — <n> due, <n> published, <n> failed` (plus filenames for anything published or failed)

---
